// import { resolveAuthToken } from "@middlewares/custom.mw"
import { Router } from "express"

const route = Router()

// route.post("/", resolveAuthToken, async (req, res) => {})

export default route
